// 補貨管理頁面 JavaScript

// 定義全局初始化函數
function initReorderPage() {
    console.log('初始化補貨管理頁面');
    // 補貨管理頁面的初始化邏輯
    // 例如：載入補貨列表、設置事件監聽器等
}

// 當頁面直接載入時執行初始化
document.addEventListener('DOMContentLoaded', function() {
    initReorderPage();
}); 