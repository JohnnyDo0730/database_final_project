// 採購記錄頁面 JavaScript

// 定義全局初始化函數
function initPurchase_recordPage() {
    console.log('初始化採購記錄頁面');
    // 採購記錄頁面的初始化邏輯
    // 例如：載入採購記錄列表、設置事件監聽器等
}

// 當頁面直接載入時執行初始化
document.addEventListener('DOMContentLoaded', function() {
    initPurchase_recordPage();
}); 