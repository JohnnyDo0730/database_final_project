// 購物車頁面 JavaScript

// 定義全局初始化函數
function initCartPage() {
    console.log('初始化購物車頁面');
    // 購物車頁面的初始化邏輯
    // 例如：載入購物車列表、設置事件監聽器等
}

// 當頁面直接載入時執行初始化
document.addEventListener('DOMContentLoaded', function() {
    initCartPage();
}); 